scanf("%d", &answer);
        
        if (answer == 2) {
            printf("\n🎉 정답입니다! 2단계 게이트가 열립니다!\n\n");
        } else {
            printf("\n😢 아쉽습니다. 다시 생각해보세요! (힌트: ㅂㅅ)\n\n");
        }
    }
    
    // 다음 단계를 위해 정답 변수를 다시 초기화해줍니다.
    answer = 0; 
    
    // ================= [ STAGE 2 ] =================
    while (answer != 1) {
        printf("==================================\n");
        printf("   ★ 리눅스 수수께끼 [2단계] ★   \n");
        printf("==================================\n");
        printf("Q. 리눅스 터미널에서 '현재 내가 있는 폴더의 위치'를 알려주는 명령어는?\n");
        printf("1) pwd    2) ls    3) cd\n");
        printf("정답 번호를 입력하세요: ");
        
        scanf("%d", &answer);
        
        if (answer == 1) {
            printf("\n👑 대단합니다! 모든 리눅스 관문을 통과하셨습니다!\n\n");
        } else {
            printf("\n😢 틀렸습니다! 2일 차에 배운 명령어를 떠올려보세요!\n\n");
        }
    }
    
    return 0;
}
